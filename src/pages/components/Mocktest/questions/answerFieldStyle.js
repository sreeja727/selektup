// Shared Chakra styling for every dedicated question-answer component in
// this folder — matches the visual language already used across the exam
// screens (2px border, #039BE5 focus ring).
export const answerFieldStyle = {
  border: "2px solid",
  borderColor: "gray.200",
  borderRadius: "lg",
  _focus: { borderColor: "#039BE5", boxShadow: "0 0 0 3px rgba(3,155,229,0.12)" },
};
